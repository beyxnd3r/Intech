apiVersion: v1
kind: Service
metadata:
  name: comments-service
spec:
  selector:
    app: comments-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8278
  type: NodePort
